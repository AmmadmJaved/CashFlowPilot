# ExpenseShare Environment Variables for Vercel Deployment

# Database Configuration (Required)
DATABASE_URL=postgresql://username:password@host:5432/database
PGHOST=your-db-host.com
PGPORT=5432
PGUSER=your-username
PGPASSWORD=your-password
PGDATABASE=your-database-name

# Session Secret (Required - Generate a strong random string)
SESSION_SECRET=your-super-secret-session-key-minimum-32-characters-long

# Replit Authentication (Optional - Remove if not using Replit Auth)
REPLIT_DOMAINS=your-domain.vercel.app
ISSUER_URL=https://replit.com/oidc
REPL_ID=your-repl-id

# SendGrid Email Service (Optional)
SENDGRID_API_KEY=your-sendgrid-api-key

# Environment
NODE_ENV=production
VERCEL=1

# Vercel Configuration
VERCEL_URL=your-project.vercel.app
VERCEL_REGION=us-east-1